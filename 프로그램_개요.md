# 오손(OSON) — 카톡 AI 예약직원 프로그램 개요

## 한 줄 소개
사장님 대신 카카오톡 채널에서 24시간 손님 문의에 답하고 예약 결제 링크까지 안내하는 AI 직원.
1호 고객: 뚝섬 수상레저 업체 **서퍼스트** (파일럿 운영 중, 오류 안정화되면 유료 전환 예정).

## 스택 / 배포
- Python + FastAPI + uvicorn, 배포: **Railway** (main 푸시 = 자동 배포)
- LLM: Anthropic **claude-haiku-4-5** (주) / **claude-sonnet-5** (폴백)
- DB: Supabase (예약·잔여석·리드) / 알림: Slack webhook / 날씨: 기상청(KMA) API
- 총 ~5,900줄 (app/), 테스트: pytest (tests/)

## 구조
```
app/
├── main.py            # 앱 조립 + lifespan 백그라운드 루프 (캐시 갱신·워밍)
├── config.py          # 환경변수 (ANTHROPIC_API_KEY, SUPABASE, OPS_TOKEN 등)
├── routers/
│   ├── kakao.py       # ★ 카톡 오픈빌더 웹훅 (동기 4.2s / 콜백 50s 모드)
│   ├── landing.py     # 오손 홍보 랜딩 (코럴 #f4511e, 카톡 밤 채팅방 데모) + 리드 수집
│   ├── admin.py       # 사장님 관리 페이지 (bcrypt 인증)
│   ├── availability.py# 잔여석 관리 API
│   ├── photos.py      # 손님 사진 업로드/앨범 (크기 제한 없음 — DSLR용)
│   ├── dashboard.py   # 운영 대시보드
│   ├── ops.py         # 자율 운영 점검 엔드포인트 (OPS_TOKEN 인증)
│   └── health.py      # 헬스체크 (10분 주기 모니터링 대상)
└── services/
    ├── agent.py       # ★ 핵심 두뇌 (아래 상세)
    ├── availability.py# 잔여석 텍스트 빌드 (Supabase)
    ├── weather.py     # KMA 날씨 → 운영 가능 판단
    ├── alerts.py      # FailureAlarm — 연속실패 N회 시 슬랙 경보 (외부의존성 전체 적용)
    ├── slack.py       # 슬랙 발송
    ├── album.py / naver.py / db.py / auth.py
```

## 핵심 두뇌: services/agent.py

### 설계 원칙: 카톡 5초 타임아웃이 1순위
- 요청 경로에서 외부 API 호출 절대 금지. 날씨·잔여석은 **백그라운드 루프가 미리 캐시**
  (날씨 30분·잔여석 1분 주기), 요청 시엔 캐시만 즉시 읽음 → 추가 지연 0초.
- **프롬프트 캐시 워밍**: 4분마다 max_tokens=1 요청으로 Anthropic 캐시(TTL 5분) 유지
  → 첫 메시지도 항상 캐시 히트로 즉답.

### 응답 파이프라인 (get_reply)
1. 시스템 프롬프트 = 정적 블록(캐시: 업체정보·가격·링크·응대규칙·[언어] 다국어 지시)
   + 동적 블록(오늘 날짜, 서버 계산 날짜-요일 달력 3주, 날씨, 잔여석)
2. **재시도 7회**: 주모델↔폴백모델 교대 + backoff(0→12s), deadline 내에서만
   (동기 모드 4.2s는 1~2회, 콜백 모드 50s는 전체 소진 → 오버로드 흡수)
3. 전멸 시 **정적 폴백**: 가격/사진/예약/취소 단골 질문은 서버가 직접 답변 (한국어 고정)
4. 그래도 없으면 자연스러운 안내문 ("문의가 많아 늦어지고 있어요" + 전화번호)
5. 대화 기록: 유저당 10턴, 최대 5,000유저 LRU (인메모리)

### 축적된 사고 대응 규칙 (프롬프트에 박제)
- 요일 직접 계산 금지 — 서버 달력에서만 읽기 (7/17 요일 언쟁 사고)
- 안 물어본 날짜·종목 잔여석 나열 금지 (TMI 사고)
- 잔여 좌석 숫자 노출 금지, 마크다운 금지 (카톡 미지원)
- URL은 절대 중간에 자르지 않기
- 예약 확정은 직접 하지 않고 "사장님 확인 후 안내"
- 사진 수령 문의 → 사장님 연락처 안내

## 예약 흐름
문의 → AI가 날짜·종목 가능/불가 판단 → **네이버 스마트스토어 결제 링크** 즉시 제공
(패들보드/카약·전동e포일·스냅사진·윈드서핑은 링크, E포일/펌핑포일은 전화 유도)

## 자율 운영 (사장님 개입 없이 돌아가는 것들)
- 10분 주기 헬스체크 / 매일 8시 운영점검 / 매주 월 9시 전략회의 루틴 (OPS_TOKEN)
- FailureAlarm: 외부의존성(Anthropic·KMA·Supabase) 연속실패 시 슬랙 즉시 경보
  (예: Anthropic 2연속 실패 = 8분 내 감지 → "크레딧 확인하세요" 힌트 포함)

## 랜딩 / 브랜드
- 루트(/) = 오손 홍보 랜딩: "카톡 AI 예약직원" 포지셔닝 (업종 나열 금지)
- 브랜드 색: 코럴 오렌지 #f4511e (카톡 노랑은 카톡 화면 묘사에만)
- 리드 수집 파이프라인 가동 중

## 현재 상태 (2026-08-02)
- **장애 중: Anthropic 크레딧 소진** → 충전 + auto-reload 설정 필요 (상세: 작업내용_2026-08-02.md)
- 로컬 미커밋: availability 상태 API 개선 3파일 + 테스트 + 사장님_사용가이드.md
- 장기 비전: 솜씨당/스마트스토어형 플랫폼화 (무사업자 강사 입점)
